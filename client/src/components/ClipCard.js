// imports

const ClipCard = ( {id, title, link, notes} ) => {

  
  return (
    <>
      <div>{title}</div>
      <div>{link}</div>
      <div>{notes}</div>
    </>
  );
};

export default ClipCard
