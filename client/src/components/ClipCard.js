// imports

const ClipCard = ({ title, link, notes }) => {

  
  return (
    <>
      <div>{title}</div>
      <div>{link}</div>
      <div>{notes}</div>
      
    </>
  );
};

export default ClipCard
